PORT=5000
MONGODB_URI=mongodb://localhost:27017/codecanvas
# Example MongoDB Atlas connection (replace <PASSWORD> and <DBNAME> accordingly):
# mongodb+srv://khushi:<PASSWORD>@cluster0.fb1rvlc.mongodb.net/<DBNAME>?retryWrites=true&w=majority
# If your password contains special characters, url-encode it (e.g. use %40 for @)
JWT_SECRET=your_jwt_secret_key_here_change_in_production
NODE_ENV=development

# Quick guidance:
# - For local development keep MONGODB_URI as the localhost URL.
# - For Render/production, set the MONGODB_URI environment variable to your Atlas connection string
#   (e.g. mongodb+srv://khushi:YOUR_PASSWORD@cluster0.fb1rvlc.mongodb.net/codecanvas?retryWrites=true&w=majority)
